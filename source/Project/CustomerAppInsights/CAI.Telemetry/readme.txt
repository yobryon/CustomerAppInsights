# Telemetry library

## tier 1 dev box preparation

To be able to build this library on a cloud-hosted tier 1 dev box, you will need to add the online nuget package provider so that VS can restore the nuget packages required for appinsights.
